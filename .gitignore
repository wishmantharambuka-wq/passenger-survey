# dependencies
node_modules
.pnp
.pnp.js

# next.js build output — must never be committed; committing it causes stale
# build artifacts and file-lock errors on Windows
.next
out
build

# env — never commit keys
.env
.env.local
.env*.local

# generated sample exports
scripts/sample_Node_*.csv

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts
